interface:
  display_name: "Prompt Upgrade"
  short_description: "Audit and upgrade prompts for GPT-6 Astra"
  default_prompt: "Use $prompt-upgrade to audit this prompt and rewrite it for GPT-6 Astra without changing its intent."
policy:
  allow_implicit_invocation: false
