# Repository Guidance

## Commit Messages

All agents must use Conventional Commits for every commit in this repository.

Use the format `type(optional-scope): description`. Common types include `feat`, `fix`, `docs`, `refactor`, `test`, and `chore`.
